[package]
name = "kosim_input"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { version = "0.17.3", features = [
    "dynamic_linking",
    "file_watcher",
    "embedded_watcher",
    "bevy_dev_tools",
] }
kosim_utility = { version = "0.1.0", path = "../kosim_utility" }
