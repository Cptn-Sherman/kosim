[package]
name = "kosim_terrain"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { version = "0.17.3", features = [
    "dynamic_linking",
    "file_watcher",
    "embedded_watcher",
    "bevy_dev_tools",
] }